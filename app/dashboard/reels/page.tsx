import { createSupabaseServerClient } from '../../../lib/supabase/server'
import PostCard from '../../../components/social/PostCard'
export default async function ReelsPage(){const supabase=await createSupabaseServerClient();const {data}=await supabase.from('posts').select('*,profiles(username,avatar_url)').eq('type','reel').order('created_at',{ascending:false}).limit(60);return <section className="mx-auto max-w-[1400px]"><div className="mb-7"><p className="text-[10px] font-bold uppercase tracking-[.3em] text-[#833AB4]">Motion / Reels</p><h1 className="mt-2 text-4xl font-black md:text-5xl">Commercial motion.</h1><p className="mt-2 text-sm text-white/40">Fullscreen-ready video work with campaign telemetry.</p></div><div className="grid gap-5 sm:grid-cols-2 xl:grid-cols-3 2xl:grid-cols-4">{(data??[]).map((post:any)=><PostCard key={post.id} post={post}/>)}</div></section>}